[defaults]
roles_path = ansible/roles
# 默认 inventory 为 dev；管理面 Hub 须显式指定：-i ansible/inventories/mgmt/
inventory = ansible/inventories/dev/
host_key_checking = False
retry_files_enabled = False
local_tmp = .ansible/tmp

[ssh_connection]
pipelining = True
