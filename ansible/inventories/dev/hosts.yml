---
# CI 与 Dev 跨 VPC：ansible_host 见 host_vars/<host>.yml（由 network.dev_hosts 驱动）
all:
  children:
    dev:
      children:
        dev_app:
          hosts:
            dev-01:
        dev_worker:
          hosts:
            dev-02:
    jumpserver_assets:
      hosts:
        dev-01:
        dev-02:
