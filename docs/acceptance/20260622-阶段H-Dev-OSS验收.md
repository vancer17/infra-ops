# 阶段 H：Dev OSS（`infra-dev-file-storage`）验收报告

> **验收日期**：2026-06-22  
> **执行位置**：ci-01 / dev-01 同机（`121.41.58.20` / `172.21.226.38`）  
> **原始日志**：`logs/console-acceptance.log`（OSS smoke 段落）  
> **策略**：独立 Dev 桶 `infra-dev-file-storage`（资源组 `InfraDevOss`）+ RAM 自定义策略 `DevOssInfraDevFileStorageOnly`（显式 Deny `yzx-file-storage`）

---

## 一、验收结论

| 项 | 结果 |
|----|------|
| 总体 | **通过** — Dev 对象存储层可用于后续应用接入 |
| Dev Bucket | `infra-dev-file-storage`（杭州，ZRS，版本控制已开） |
| RAM 角色 | `Dev-ECS-Role`（ECS 实例元数据 `Code: Success`） |
| 访问方式 | `EcsRamRole` + 内网 Endpoint `oss-cn-hangzhou-internal.aliyuncs.com` |
| 生产隔离 | `yzx-file-storage` list / put **均被拒绝** |

---

## 二、检查项明细

| # | 检查 | 命令/方式 | 结果 |
|---|------|-----------|------|
| 1 | 依赖 | `make oss-smoke-deps` | curl、python3、ossutil v1.7.19 OK |
| 2 | RAM 元数据 | `make oss-smoke-meta` | 角色 `Dev-ECS-Role`，STS `Code: Success` |
| 3 | Dev 桶 Put | `oss://infra-dev-file-storage/smoke-test/20260622-ecs-bind.txt` | 上传成功 |
| 4 | Dev 桶 Get | 下载至临时文件 + `cmp` | 内容一致 |
| 5 | Dev 桶 List | `smoke-test/` 前缀列举 | 可见 smoke 对象 |
| 6 | 生产桶 List | `oss://yzx-file-storage/` | 拒绝（OK） |
| 7 | 生产桶 Put | `smoke-test-must-fail.txt` | 拒绝（OK） |
| 8 | 全程无 AK/SK | `~/.ossutilconfig` 为 `EcsRamRole` 模式 | OK |

**自动化入口**：`make oss-smoke`（`scripts/dev/oss-smoke.sh local all`）

---

## 三、架构要点（相对初版 OSS 规划之变更）

| 项 | 初版（2026-06-16） | 当前实机（2026-06-22） |
|----|-------------------|------------------------|
| Dev 数据落点 | `yzx-file-storage` + `dev/` 前缀 | **独立桶** `infra-dev-file-storage` |
| 与生产隔离 | 前缀 + RAM 最小权限 | **桶级隔离** + 策略 Deny `yzx-file-storage` |
| 验收脚本 | 手工 ossutil | **`make oss-smoke`**（阶段 H4 脚本封装） |

---

## 四、台账同步（本验收后须一致）

| 文件 | 变更要点 |
|------|----------|
| `ansible/inventories/dev/group_vars/all/main.yml` | `oss.bucket` → `infra-dev-file-storage`；`oss.prefix` → `""` |
| `docs/assets/registry.yaml` | `shared_cloud_dependencies.oss.dev_bucket` |
| `docs/assets/dev-01.yaml` | `ram_role.attached: true`；`dependencies.oss`；`oss_dev` |
| `docs/assets/ci-01.yaml` | `ram_role.attached: true`（同机） |
| `docs/oss/20260616-OSS-实例现状与Dev规划.md` | §二～§十 更新为已验收态 |
| `docs/dev/开发环境介绍-业务部署指南.md` | 第六节 OSS 变量与路线图 |

---

## 五、待办（不阻塞本验收）

| 项 | 说明 |
|----|------|
| dev-02 Bootstrap | 绑定同一 `Dev-ECS-Role` 后重复 `make oss-smoke` |
| `taoyouxiangzu` | 业务确认后新建或从台账移除 |
| 应用 `.env` | 业务接入时配置 `OSS_BUCKET=infra-dev-file-storage`（由 Ansible `env.j2` 生成） |
| smoke 对象清理 | 可选删除 `smoke-test/` 下历史探测文件 |

---

## 六、相关文档

- [OSS 实例现状与 Dev 规划](../oss/20260616-OSS-实例现状与Dev规划.md)
- [开发环境业务部署指南](../dev/开发环境介绍-业务部署指南.md)
- `scripts/dev/oss-smoke.sh` · `make oss-smoke`
